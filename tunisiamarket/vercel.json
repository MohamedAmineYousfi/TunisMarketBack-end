{
    "version": 2,
    "builds": [
        {
            "src": "build/main.js",
            "use": "@vercel/node"
        }
    ],
    "rewrites": [{ "source": "/(.*)", "destination": "/" }] ,
    "routes": [
        {
            "src": "/(.*)",
            "dest": "build/main.js",
            "methods": [ "GET", "POST", "PUT", "PATCH", "DELETE" ]
        }
    ]
}
