{
    "version": 2,
    "builds": [
        {
            "src": "tunisiamarket/src/main.js",
            "use": "@vercel/node"
        }
    ],
    "rewrites": [{ "source": "/(.*)", "destination": "/" }] ,
    "routes": [
        {
            "src": "/(.*)",
            "dest": "tunisiamarket/src/main.js",
            "methods": [ "GET", "POST", "PUT", "PATCH", "DELETE" ]
        }
    ]
}
